# 第二批游戏验收报告

> 验收日期：2026-04-18  
> 验收范围：第二批 3 个游戏 + 首页新卡片

---

## 整体结论：✅ 通过，1 处交互降级已补

---

## 结构检查

| 检查项 | 结果 |
|--------|------|
| 3 个游戏目录均存在，文件体量正常（492–743 行） | ✅ |
| 所有路径均用 `../../shared.css` / `../../game-utils.js` | ✅ |
| G_TOTAL = 5，`completeLevel` 触发链完整 | ✅ |
| 首页三张新卡片已全部链接（gmp_scheduler / mysql_lock / rate_limiter） | ✅ |
| `completeLevel` 全部从反馈区「继续」按钮触发，不直接跳关 | ✅ |

---

## 逐游戏验收

### ⑤ GMP 调度模拟器 `go/gmp_scheduler/` · 743 行

| 检查项 | 结果 |
|--------|------|
| 主题色 `#00d4ff`（Go 品牌青）| ✅ |
| intro + 5 关 + complete 屏幕结构完整 | ✅ |
| L1：拖拽 G/M/P 标签到对应定义框 | ✅ |
| L2：点击 G 让 P 的 M 取出执行，先本地队列 → 全局队列顺序 | ✅ |
| L3：Work Stealing 动画 — `chip-fly` keyframe，G 从队列"飞"到另一个 P | ✅ |
| L4：Hand Off — M 陷入 syscall → P 解绑 → 换绑新 M，按钮步骤序列 | ✅ |
| L5：G0/M0 特殊角色，判断 + 填空题 | ✅ |

---

### ⑥ MySQL 锁冲突判断 `mysql/mysql_lock/` · 492 行

| 检查项 | 结果 |
|--------|------|
| 主题色 `#7c83ff`（紫）| ✅ |
| intro + 5 关 + complete 屏幕结构完整 | ✅ |
| L1：给出 SQL，选全局锁 / 表锁 / 行锁（FTWRL、LOCK TABLES、FOR UPDATE）| ✅ |
| L2：双事务时间轴可视化，判断事务 B 等待 or 立即执行 | ✅ |
| L3：数字轴标注间隙锁范围 | ✅（已由选项→拖动端点补强，见下） |
| L4：Next-Key Lock 范围判断（左开右闭区间）| ✅ |
| L5：死锁复现步骤序列，判断第几步死锁 + 谁被回滚 | ✅ |

**⚠️ 交互降级（已补）：**  
L3 原实现为"从选项中选择正确区间"，roadmap 规划为"在数字轴上标出间隙锁范围"。  
已改为：数字轴上放置两个可拖动端点，拖动后区间高亮，提交时校验范围是否正确。

---

### ⑦ 流量风暴生存 `distributed/rate_limiter/` · 729 行

| 检查项 | 结果 |
|--------|------|
| 主题色 `#f59e0b`（橙）| ✅ |
| intro + 5 关 + complete 屏幕结构完整 | ✅ |
| L1：按业务特征（允许突发 / 匀速 / 精确计数）选限流算法 | ✅ |
| L2：固定窗口边界问题可视化，拖动时间滑块观察临界 2 倍流量 | ✅ |
| L3：输入令牌桶桶大小 + 填充速率，验证参数正确性 | ✅ |
| L4：熔断三态状态机（Closed / Half-Open / Open），输入错误率判断转换 | ✅ |
| L5：双滑块（限流 QPS 阈值 + 熔断触发错误率）+ canvas 实时折线渲染；通关条件：峰值 QPS ≤ 250 且错误率 ≤ 5% | ✅ |

---

## Backlog

| 优先级 | 游戏 | 问题 | 状态 |
|--------|------|------|------|
| 低 | MySQL Lock L3 | 选项选区间 → 规划数字轴拖动标注 | ✅ 已补：数字轴双端点拖动，区间实时高亮 |

---

## 下一步

第三批（按需开发）：

```
⬜ ⑧  mysql/mysql_index      — SQL 查询路径追踪器（5 关）
⬜ ⑨  redis/redis_ha          — Redis 高可用演练（5 关）
⬜ ⑩  network/tcp_handshake   — TCP 握手挥手模拟器（5 关）
⬜ ⑪  distributed/dist_lock   — 分布式锁方案裁判（4 关）
⬜ ⑫  mysql/mysql_recovery    — MySQL 崩溃恢复（4 关）
⬜ ⑬  distributed/mongodb_selector — MongoDB 场景选型（4 关）
```

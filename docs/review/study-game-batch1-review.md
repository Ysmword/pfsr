# 第一批游戏验收报告

> 验收日期：2026-04-18  
> 验收范围：第一批 4 个新游戏 + 2 个迁移游戏 + 首页更新

---

## 整体结论：✅ 通过，2 处交互降级已记录

---

## 结构检查

| 检查项 | 结果 |
|--------|------|
| 6 个游戏目录均存在（4 新建 + 2 迁移） | ✅ |
| 所有子目录游戏路径均用 `../../shared.css` / `../../game-utils.js` | ✅ |
| MVCC 迁移至 `mysql/mvcc/`，路径修正正确 | ✅ |
| IO Multiplexing 迁移至 `network/io_multiplexing/`，路径修正正确 | ✅ |
| 首页 5 个分类全部存在 | ✅ |
| 未开发游戏显示「即将推出」灰色锁定卡片 | ✅ |
| `completeLevel` 全部从反馈区「继续」按钮触发，不直接跳关 | ✅ |

---

## 逐游戏验收

### ① GC 三色标记模拟器 `go/gc_simulator/` · 765 行

| 检查项 | 结果 |
|--------|------|
| 主题色 `#10b981`（绿） | ✅ |
| intro + 5 关 + complete 屏幕结构完整 | ✅ |
| L1：9 个对象逐一点选三色 | ✅ |
| L2：SVG 有向图 + JS 动态节点 + 点击步骤序列，颜色实时变化 | ✅ |
| L3：写屏障触发判断（插入屏障 vs 删除屏障） | ✅ |
| L4：6 个对象操作场景，判断混合写屏障下最终颜色 | ✅ |
| L5：三代 GC 各阶段分类（STW / 并发 / 无此阶段），含解释对比 | ✅ |
| `completeLevel` 全部从「继续」按钮触发 | ✅ |

**⚠️ 交互降级 × 2（记入 backlog）：**
- **L1**：规划为「拖拽对象到三个颜色区域」，实现为「点击对象选颜色」。功能等价，少了 drag-and-drop 体感。
- **L5**：规划为「拖拽 STW/并发标记/写屏障触发事件到时间段」，实现为「每行点选阶段类型」。逻辑完整，视觉冲击弱于拖拽时间轴。

---

### ② 并发 Bug 代码审判 `go/concurrency_court/` · 699 行

| 检查项 | 结果 |
|--------|------|
| 主题色 `#ef4444`（红） | ✅ |
| intro + 6 关 + complete 屏幕结构完整 | ✅ |
| L1：Mutex 死锁判断 + 原因选择 | ✅ |
| L2：RWMutex 升级陷阱 | ✅ |
| L3：WaitGroup 误用判断 | ✅ |
| L4：Channel 关闭 4 段代码逐一判断行为 | ✅ |
| L5：defer 顺序两步交互（是否 panic + 拖排执行顺序） | ✅ |
| L6：select 盲点 —— nil channel + 两个 case 均 ready 时随机选择追加题 | ✅ |
| 每关底部有「提交判决」，提交后显示「裁判词」（全文共 41 处） | ✅ |
| `completeLevel` 全部从「继续」按钮触发 | ✅ |

---

### ③ 协程泄漏侦探 `go/goroutine_detective/` · 613 行

| 检查项 | 结果 |
|--------|------|
| 主题色 `#f59e0b`（琥珀） | ✅ |
| intro + 5 关 + complete 屏幕结构完整 | ✅ |
| L1：忘记 cancel —— 点击高亮泄漏位置 + 选修复方案 | ✅ |
| L2：channel 无人关闭 —— 判断谁关闭 + 何时关闭 | ✅ |
| L3：WithTimeout 未感知 —— 判断是否泄漏 + 生命周期 | ✅ |
| L4：context 树可视化，点击取消节点后子 goroutine 逐个变灰退出 | ✅ |
| L5：综合案例 —— HTTP Handler + goroutine + channel + context 全场景 | ✅ |
| `completeLevel` 全部从「继续侦查」按钮触发 | ✅ |

---

### ④ Redis 故障诊断室 `redis/redis_diagnosis/` · 642 行

| 检查项 | 结果 |
|--------|------|
| 主题色 `#f97316`（橙红） | ✅ |
| intro + 5 关 + complete 屏幕结构完整 | ✅ |
| L1：QPS 曲线 + 慢查询日志 → 四种故障选诊断结论 | ✅ |
| L2：热 key 处理 —— 4 方案选最优并说明副作用 | ✅ |
| L3：大 key 处置 —— DEL / UNLINK / HSCAN 分批选择 | ✅ |
| L4：缓存击穿诊断 —— 穿透/击穿/雪崩区分 + singleflight vs mutex | ✅ |
| L5：5 条 slowlog 逐条打标签（含 `KEYS *`、大 key、全量扫描）+ 优化方案 | ✅ |
| `completeLevel` 全部从「继续」按钮触发 | ✅ |

---

## Backlog（已全部解决）

| 优先级 | 游戏 | 问题 | 状态 |
|--------|------|------|------|
| 低 | GC Sim L1 | 点击选色 → 规划拖拽 | ✅ 已改为 HTML5 drag & drop：9 个对象卡片从对象池拖入⬜白 / ◑灰 / ⬛黑三个分区，答错后卡片内显示正确颜色提示 |
| 低 | GC Sim L5 | 点选阶段类型 → 规划拖拽时间轴 | ✅ 已改为芯片拖拽：⛔STW / ✅并发 / —无此阶段 三种芯片可重复拖入对应格子，点击已放置芯片可清除重拖，提交后红色格子展示正确答案 |

---

## 下一步

第二批（调度 + 数据库深度）：

```
⬜ ⑤ go/gmp_scheduler       — GMP 调度模拟器（5 关）
⬜ ⑥ mysql/mysql_lock        — MySQL 锁冲突判断（5 关）
⬜ ⑦ distributed/rate_limiter — 流量风暴生存（5 关）
```
